# 项目协作规则

## 任务开始前

1. 在修改任何文件前执行 `git fetch --all --prune --tags`，确认远端可正常访问。
2. 比较本地当前分支与 `origin`、`zing` 对应分支的提交版本。
3. 远端存在新提交时，使用快进方式拉取并再次确认版本一致。
4. 获取、拉取或版本校验失败时立即停止本次任务，不进行文件修改。
5. 保留用户尚未提交的本地改动，不覆盖、不回退与本次任务无关的内容。

## 任务完成后

1. 根据改动范围完成语法检查、自动化测试、构建或运行验证。
2. 使用中文提交说明提交本次改动。
3. 将当前分支推送到 `origin` 和 `zing`；本次新增版本标签时，同时推送标签。
4. 推送后比较本地、`origin` 和 `zing` 的提交哈希，确认三者一致。
5. 提交或推送失败时如实记录失败步骤，不把任务标记为已完成。
